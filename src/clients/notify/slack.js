// Slack notification channel — stub.
// Implement when Slack integration is ready.

export function create(opts) {
  return {
    comment(issueId, { body }) {
      // No-op until implemented
    },
  };
}
